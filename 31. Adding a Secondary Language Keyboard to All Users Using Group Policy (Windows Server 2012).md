## 📌 Active Direcory: Adding a Secondary Language (Nepali Language) Keyboard to All Users Using Group Policy (Windows Server 2012)

### 📖 Overview

This guide provides **step-by-step instructions** to deploy a secondary language keyboard (Nepali) to all users in a domain using **Group Policy** on Windows Server 2012.

It covers:

* **Server-side configuration** (creating and configuring GPO)
* **Client-side application** (updating policy and verifying)

---

## 🏢 Server-Side Configuration (Group Policy)

Perform the following steps on the **Domain Controller**:

1. 🔹 **Open Server Manager**

2. 🔹 **Click Tools → Group Policy Management**

3. 🔹 **Select the server name** (e.g., `adserver.local`)

4. 🔹 **Navigate to the appropriate OU** (e.g., `Domain Users`)

5. 🔹 **Create a new GPO and link it to the OU**

6. 🔹 **Name the GPO:** `Adding Nepali languages as a secondary Keyboard`

7. 🔹 **Edit the GPO**

8. 🔹 **Navigate to Registry Settings:**

   ```
   User Configuration > Preferences > Windows Settings > Registry
   ```

   * Right-click **New** → **Registry Item**

9. 🔹 **Configure the Registry Item:**

   * **Action:** Update
   * **Hive:** HKEY_CURRENT_USER
   * **Key Path:** `Keyboard Layout\Preload`
   * **Enable:** Default
   * **Value Type:** REG_SZ
   * **Value Data:** `00000461` (Nepali keyboard code)

10. 🔹 **Click Apply → OK**

11. 🔹 **Open Command Prompt as Administrator**

12. 🔹 **Force Group Policy Update:**

    ```cmd
    gpupdate /force
    ```

---

## 💻 Client-Side Configuration

After applying the policy on the server, perform the following steps on client computers:

1. 🔹 **Open Command Prompt as Administrator**
2. 🔹 **Force Group Policy Update:**

   ```cmd
   gpupdate /force
   ```
3. 🔹 **Reboot the system**
4. 🔹 **Verify Keyboard Layout:**

   * Navigate to **Keyboard Settings** → check that **Nepali Keyboard** is available as a secondary layout.

---

## 📝 Notes

* ⚠️ Users may need to **log out and log back in** for the keyboard to appear.
* 🆔 `00000461` is the **standard registry code for Nepali keyboard layout** in Windows.
* ✅ This GPO ensures all **domain users automatically receive** the Nepali keyboard without manual configuration.

---
