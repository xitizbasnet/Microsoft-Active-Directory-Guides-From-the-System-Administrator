## 🚫 Disable Microsoft Teams Startup Using Group Policy (Windows)

---

## 📌 Document Overview

This manual provides standardized, enterprise-ready instructions for disabling **Microsoft Teams automatic startup** on Windows client devices using **Group Policy Objects (GPO)** and **Registry Preferences**. 

---

## 🎯 Objective

* Prevent Microsoft Teams from launching automatically at user logon
* Apply configuration centrally using Group Policy
* Validate configuration through registry and client-side verification

---

## 🛠️ Pre‑Change Verification (Registry Check)

Before applying the Group Policy, verify the existing registry configuration on a client system.

📂 **Registry Location**

Hive:
`HKEY_CURRENT_USER\Software\Classes\Local Settings\Software\Microsoft\Windows\CurrentVersion\AppModel\SystemAppData\MSTeams_8wekyb3d8bbwe\TeamsTfwStartupTask`

🔎 **Expected Registry Value (Before Policy)**

* Value Name: `State`
* Value Data: `0x00000002 (2)`

This value indicates that Microsoft Teams is currently enabled to run at startup.

---

## 🖥️ Group Policy Configuration

### Step 1: Open Group Policy Management

1. Open **Server Manager**
2. Click **Tools** → **Group Policy Management**

---

### Step 2: Create a New Group Policy Object

📍 Navigate to the Organizational Unit (OU) where the target users are located.

Example:

* Domain: `xitiztechservices.local`

📂 Steps:

* Go to **Group Policy Objects**
* Right‑click → **New**
* Name the GPO:
  **`Disable startup Microsoft Teams`**
* Right‑click the newly created GPO → **Edit**

---

### Step 3: Configure Registry Preference

📁 Navigate to:

`User Configuration → Preferences → Windows Settings → Registry`

➕ Right‑click → **New** → **Registry Item**

---

## ⚙️ Policy Settings

Configure the registry item as follows:

* **Action:** Update
* **Hive:** HKEY_CURRENT_USER
* **Key Path:**
  `SOFTWARE\Classes\Local Settings\Software\Microsoft\Windows\CurrentVersion\AppModel\SystemAppData\MSTeams_8wekyb3d8bbwe\TeamsTfwStartupTask`
* **Value Name:** `State`
* **Value Type:** `REG_DWORD`
* **Value Data:** `1`
* **Base:** Hexadecimal

✅ Click **Apply** → **OK**

---

## 🧩 GPO Status Configuration

To ensure this policy applies only at the user level:

1. Select the GPO **Disable startup Microsoft Teams**
2. Go to **Details** tab
3. Set **GPO Status** to:
   **Computer configuration settings disabled**
4. Click **OK**

---

## 🔄 Apply Policy on Client Machines

On the client system, open **PowerShell** and run:

```
gpupdate /force
```

This forces the immediate application of the updated Group Policy.

---

## ✅ Post‑Change Verification (Registry Check)

Re‑check the registry on the client device.

📂 **Registry Location**

`HKEY_CURRENT_USER\Software\Classes\Local Settings\Software\Microsoft\Windows\CurrentVersion\AppModel\SystemAppData\MSTeams_8wekyb3d8bbwe\TeamsTfwStartupTask`

🔎 **Expected Registry Value (After Policy)**

* Value Name: `State`
* Value Data: `0x00000001 (1)`

This confirms that Microsoft Teams startup has been successfully disabled.

---

## 👤 End‑User Validation

* Log in as the affected user
* Open **Task Manager** → **Startup** tab
* Confirm that **Microsoft Teams** is **Disabled**

---

## 📎 Notes

* This configuration applies per user (HKCU)
* Suitable for environments using the Microsoft Store (MSIX) version of Teams
* Recommended for performance optimization and controlled application startup

---


